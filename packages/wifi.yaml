wifi:
  ap:
  on_connect:
    - ble.disable:
  

text_sensor:
  - platform: wifi_info
    # entity_category: "diagnostic"
    ip_address: 
      name: "IP Address"
    ssid:
      name: "SSID"

# Enable Improv
esp32_improv:
  authorizer: none

# Enable the captive portal
captive_portal: